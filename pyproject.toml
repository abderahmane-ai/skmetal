[tool.ruff]
target-version = "py310"
line-length = 120
extend-exclude = ["skmetal/skmetal_bridge"]
lint.select = ["E", "F", "W", "I", "N", "UP"]
lint.ignore = ["E501", "E402", "N812"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
